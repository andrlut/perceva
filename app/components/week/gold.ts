/**
 * The sheet's gold identity — the tints shared by every surface that renders
 * one of "As 3 da Semana" (BigSlot, WeekStrip, the ritual). Kept here so the
 * feature's visual identity lives in ONE place instead of magic rgba spread
 * across three files. Derived from tokens.semantic.coin (#FFC83D).
 */
export const GOLD_TINT_BG = 'rgba(255, 200, 61, 0.07)';
export const GOLD_TINT_BG_DONE = 'rgba(255, 200, 61, 0.03)';
export const GOLD_BADGE_BG = 'rgba(255, 200, 61, 0.18)';
export const GOLD_BORDER_DONE = 'rgba(255, 200, 61, 0.18)';
